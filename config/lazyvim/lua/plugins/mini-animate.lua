return {
  'echasnovski/mini.animate',
  version = false,
  event = 'VeryLazy',
  opts = function(_, opts)
    opts.scroll = {
      enable = false,
    }
    opts.open = {
     enable = false
    }
    opts.close = {
     enable = false
    }
end,
}
